#!/usr/bin/env bash

set -e            # fail fast
set -o pipefail   # don't ignore exit codes when piping output
# set -x          # enable debugging
echo started
status "started."
build_dir=$1
cache_dir=$2
status "Done."
